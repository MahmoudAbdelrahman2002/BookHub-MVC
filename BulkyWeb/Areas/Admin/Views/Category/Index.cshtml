@model IEnumerable<Bulky.Models.Category>

<div class="container-fluid px-1 px-sm-2 px-md-3 py-2 py-md-4">
    <div class="row justify-content-center">
        <div class="col-12 col-xl-11">
            <!-- Page Header -->
            <div class="mb-3 mb-md-4 text-center">
                <h1 class="display-6 fw-bold text-primary mb-2 fs-4 fs-sm-3 fs-md-2">
                    <i class="bi bi-collection-fill me-2"></i>Category Management
                </h1>
                <p class="text-muted lead d-none d-md-block mb-0">Manage your categories with ease</p>
                <p class="text-muted small d-md-none mb-0">Manage categories efficiently</p>
            </div>

            <!-- Stats Cards Row -->
            <div class="row mb-3 mb-md-4 g-1 g-sm-2 g-md-3">
                <div class="col-6 col-md-4 col-lg-3">
                    <div class="card border-0 shadow-sm h-100" style="border-left: 3px solid #192E2F !important;">
                        <div class="card-body text-center py-2 py-sm-3 px-2">
                            <div class="d-flex align-items-center justify-content-center mb-1 mb-sm-2">
                                <div class="bg-primary bg-gradient rounded-circle p-2 p-md-3">
                                    <i class="bi bi-grid-3x3 text-white" style="font-size: 0.9rem;"></i>
                                </div>
                            </div>
                            <h5 class="card-title text-primary fw-bold mb-1" style="font-size: 1.1rem;">@Model.Count()</h5>
                            <p class="card-text text-muted small mb-0">Total Categories</p>
                        </div>
                    </div>
                </div>
                <div class="col-6 col-md-4 col-lg-3">
                    <div class="card border-0 shadow-sm h-100" style="border-left: 3px solid #4b7147 !important;">
                        <div class="card-body text-center py-2 py-sm-3 px-2">
                            <div class="d-flex align-items-center justify-content-center mb-1 mb-sm-2">
                                <div class="bg-success bg-gradient rounded-circle p-2 p-md-3">
                                    <i class="bi bi-check-circle text-white" style="font-size: 0.9rem;"></i>
                                </div>
                            </div>
                            <h5 class="card-title text-success fw-bold mb-1" style="font-size: 1.1rem;">Active</h5>
                            <p class="card-text text-muted small mb-0">Status</p>
                        </div>
                    </div>
                </div>
                <div class="col-12 col-md-4 col-lg-6">
                    <div class="card border-0 shadow-sm h-100" style="border-left: 3px solid #BD7418 !important;">
                        <div class="card-body d-flex align-items-center justify-content-between py-2 py-sm-3 px-2 px-sm-3">
                            <div class="flex-grow-1">
                                <h6 class="card-title text-primary mb-1 fw-medium" style="font-size: 0.95rem;">Quick Actions</h6>
                                <p class="card-text text-muted small mb-0 d-none d-sm-block">Manage efficiently</p>
                            </div>
                            <a asp-controller="Category" asp-action="Create" 
                               class="btn btn-primary btn-sm shadow-sm ms-2 fw-medium">
                                <i class="bi bi-plus-circle me-1"></i>
                                <span class="d-none d-sm-inline">Add</span>
                                <span class="d-sm-none">+</span>
                            </a>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Main Content Card -->
            <div class="card border-0 shadow-lg">
                <!-- Card Header -->
                <div class="card-header bg-primary bg-gradient text-white py-3 py-md-4" 
                     style="background: linear-gradient(135deg, #192E2F 0%, #3A595C 100%) !important;">
                    <div class="row align-items-center g-2">
                        <div class="col-12 col-md-6">
                            <h2 class="fs-6 fs-sm-5 fs-md-4 fw-medium mb-0">
                                <i class="bi bi-list-ul me-2"></i>Categories List
                            </h2>
                        </div>
                        <div class="col-12 col-md-6">
                            <!-- Search and Filter Controls -->
                            <div class="d-flex flex-column flex-md-row gap-2 justify-content-md-end">
                                <div class="input-group input-group-sm">
                                    <input type="text" class="form-control border-0" 
                                           id="searchInput" 
                                           placeholder="Search categories..."
                                           style="background-color: rgba(255,255,255,0.9);">
                                    <button class="btn btn-light border-0 shadow-sm" type="button">
                                        <i class="bi bi-search text-primary"></i>
                                    </button>
                                </div>
                                <a asp-controller="Category" asp-action="Create" 
                                   class="btn btn-light btn-sm shadow-sm w-100 w-md-auto fw-medium">
                                    <i class="bi bi-plus-circle me-1 text-primary"></i>
                                    <span class="d-none d-sm-inline text-primary">Create New</span>
                                    <span class="d-sm-none text-primary">New</span>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Card Body -->
                <div class="card-body p-0">
                    <!-- Mobile Card View (xs to lg) -->
                    <div class="d-lg-none">
                        @foreach (var category in Model)
                        {
                            <div class="border-bottom category-mobile-item" 
                                 style="transition: background-color 0.2s ease; border-color: rgba(25, 46, 47, 0.1) !important;">
                                <div class="p-2 p-sm-3">
                                    <div class="row align-items-center">
                                        <div class="col-8 col-sm-9">
                                            <div class="d-flex align-items-center mb-2">
                                                <div class="bg-primary bg-gradient rounded-circle p-1 p-sm-2 me-2 shadow-sm">
                                                    <i class="bi bi-folder text-white" style="font-size: 0.75rem;"></i>
                                                </div>
                                                <h6 class="card-title text-primary mb-0 fw-medium" style="font-size: 0.9rem;">@category.Name</h6>
                                            </div>
                                            <div class="d-flex flex-wrap gap-1">
                                                <span class="badge bg-primary bg-gradient small fw-medium">ID: @category.Id</span>
                                                <span class="badge bg-success bg-gradient small fw-medium">Order: @category.DisplayOrder</span>
                                            </div>
                                        </div>
                                        <div class="col-4 col-sm-3">
                                            <div class="dropdown">
                                                <button class="btn btn-outline-primary btn-sm dropdown-toggle w-100 py-2 shadow-sm" 
                                                        type="button" data-bs-toggle="dropdown" aria-expanded="false"
                                                        style="font-size: 0.8rem;">
                                                    <i class="bi bi-three-dots-vertical"></i>
                                                </button>
                                                <ul class="dropdown-menu dropdown-menu-end shadow-lg border-0">
                                                    <li>
                                                        <a class="dropdown-item py-2 text-primary" 
                                                           asp-controller="Category" asp-action="Edit" asp-route-id="@category.Id">
                                                            <i class="bi bi-pencil me-2"></i>Edit
                                                        </a>
                                                    </li>
                                                    <li><hr class="dropdown-divider"></li>
                                                    <li>
                                                        <a class="dropdown-item py-2 text-danger" 
                                                           asp-controller="Category" asp-action="Delete" asp-route-id="@category.Id">
                                                            <i class="bi bi-trash me-2"></i>Delete
                                                        </a>
                                                    </li>
                                                </ul>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>

                    <!-- Desktop Table View (lg and up) -->
                    <div class="d-none d-lg-block">
                        <div class="table-responsive">
                            <table class="table table-hover align-middle mb-0" id="categoryTable">
                                <thead style="background: linear-gradient(135deg, rgba(25, 46, 47, 0.05) 0%, rgba(75, 113, 71, 0.05) 100%);">
                                    <tr>
                                        <th scope="col" class="py-3 ps-4 text-primary fw-medium border-0" style="width: 10%;">
                                            <div class="d-flex align-items-center">
                                                <i class="bi bi-hash me-2"></i>ID
                                            </div>
                                        </th>
                                        <th scope="col" class="py-3 text-primary fw-medium border-0" style="width: 45%;">
                                            <div class="d-flex align-items-center">
                                                <i class="bi bi-tag me-2"></i>Name
                                            </div>
                                        </th>
                                        <th scope="col" class="py-3 text-primary fw-medium border-0" style="width: 20%;">
                                            <div class="d-flex align-items-center">
                                                <i class="bi bi-sort-numeric-up me-2"></i>Display Order
                                            </div>
                                        </th>
                                        <th scope="col" class="text-center py-3 pe-4 text-primary fw-medium border-0" style="width: 25%;">
                                            <div class="d-flex align-items-center justify-content-center">
                                                <i class="bi bi-gear me-2"></i>Actions
                                            </div>
                                        </th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var category in Model)
                                    {
                                        <tr class="category-table-row" style="transition: all 0.2s ease;">
                                            <td class="ps-4 border-0">
                                                <span class="badge bg-primary bg-gradient fw-medium shadow-sm">@category.Id</span>
                                            </td>
                                            <td class="border-0">
                                                <div class="d-flex align-items-center">
                                                    <div class="bg-primary bg-gradient rounded-circle p-2 me-3 shadow-sm">
                                                        <i class="bi bi-folder text-white"></i>
                                                    </div>
                                                    <span class="fw-medium text-primary">@category.Name</span>
                                                </div>
                                            </td>
                                            <td class="border-0">
                                                <span class="badge bg-success bg-gradient fw-medium shadow-sm">@category.DisplayOrder</span>
                                            </td>
                                            <td class="pe-4 border-0">
                                                <div class="d-flex justify-content-center gap-2">
                                                    <a asp-controller="Category" asp-action="Edit" asp-route-id="@category.Id" 
                                                       class="btn btn-outline-primary btn-sm shadow-sm fw-medium">
                                                        <i class="bi bi-pencil-square"></i>
                                                        <span class="d-none d-xl-inline ms-1">Edit</span>
                                                    </a>
                                                    <a asp-controller="Category" asp-action="Delete" asp-route-id="@category.Id" 
                                                       class="btn btn-outline-danger btn-sm shadow-sm fw-medium">
                                                        <i class="bi bi-trash"></i>
                                                        <span class="d-none d-xl-inline ms-1">Delete</span>
                                                    </a>
                                                </div>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>

                    @if (!Model.Any())
                    {
                        <div class="text-center py-5" 
                             style="background: linear-gradient(135deg, rgba(25, 46, 47, 0.02) 0%, rgba(75, 113, 71, 0.02) 100%);">
                            <div class="mb-4">
                                <i class="bi bi-inbox display-1 text-primary opacity-50"></i>
                            </div>
                            <h5 class="text-primary fs-5 fw-medium mb-2">No categories found</h5>
                            <p class="text-muted mb-4">Start by creating your first category to organize your content</p>
                            <a asp-controller="Category" asp-action="Create" 
                               class="btn btn-primary btn-lg shadow-sm fw-medium">
                                <i class="bi bi-plus-circle me-2"></i>Create Your First Category
                            </a>
                        </div>
                    }
                </div>

                <!-- Enhanced Card Footer -->
                <div class="card-footer border-0 py-3" 
                     style="background: linear-gradient(135deg, rgba(25, 46, 47, 0.03) 0%, rgba(75, 113, 71, 0.03) 100%);">
                    <div class="row align-items-center">
                        <div class="col-12 col-md-6">
                            <small class="text-primary d-flex align-items-center">
                                <i class="bi bi-info-circle me-2"></i>
                                <span class="fw-medium">Total: @Model.Count() categories</span>
                            </small>
                        </div>
                        <div class="col-12 col-md-6 text-md-end mt-2 mt-md-0">
                            <small class="text-muted">
                                <i class="bi bi-clock me-1"></i>
                                Last updated: @DateTime.Now.ToString("MMM dd, yyyy")
                            </small>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Mobile Quick Actions Panel -->
            <div class="d-lg-none mt-3">
                <div class="card border-0 shadow-sm" 
                     style="background: linear-gradient(135deg, rgba(189, 116, 24, 0.05) 0%, rgba(189, 116, 24, 0.1) 100%); border-left: 4px solid #BD7418 !important;">
                    <div class="card-body py-3 px-3">
                        <div class="row align-items-center g-2">
                            <div class="col-8">
                                <div class="d-flex align-items-center">
                                    <i class="bi bi-lightning me-2 text-warning"></i>
                                    <small class="text-warning fw-medium">Quick Actions</small>
                                </div>
                            </div>
                            <div class="col-4 text-end">
                                <a asp-controller="Category" asp-action="Create" 
                                   class="btn btn-warning btn-sm shadow-sm fw-medium w-100">
                                    <i class="bi bi-plus-circle me-1"></i>
                                    <span class="d-none d-sm-inline">Add New</span>
                                    <span class="d-sm-none">Add</span>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const searchInput = document.getElementById('searchInput');
            const table = document.getElementById('categoryTable');
            const mobileCards = document.querySelectorAll('.d-lg-none .category-mobile-item');
            
            // Enhanced search functionality with theme integration
            if (searchInput) {
                searchInput.addEventListener('keyup', function() {
                    const filter = this.value.toLowerCase();
                    
                    // Add visual feedback for search
                    if (filter.length > 0) {
                        this.style.borderColor = '#192E2F';
                        this.style.borderWidth = '2px';
                        this.style.backgroundColor = 'rgba(25, 46, 47, 0.05)';
                    } else {
                        this.style.borderColor = '';
                        this.style.borderWidth = '';
                        this.style.backgroundColor = 'rgba(255,255,255,0.9)';
                    }
                    
                    // Filter desktop table
                    if (table) {
                        const rows = table.getElementsByTagName('tbody')[0].getElementsByTagName('tr');
                        let visibleCount = 0;
                        
                        for (let i = 0; i < rows.length; i++) {
                            const nameCell = rows[i].getElementsByTagName('td')[1];
                            if (nameCell) {
                                const textValue = nameCell.textContent || nameCell.innerText;
                                const isVisible = textValue.toLowerCase().indexOf(filter) > -1;
                                rows[i].style.display = isVisible ? '' : 'none';
                                if (isVisible) visibleCount++;
                            }
                        }
                        
                        // Update results indicator for desktop
                        updateResultsIndicator(visibleCount, 'desktop');
                    }
                    
                    // Filter mobile cards
                    let mobileVisibleCount = 0;
                    mobileCards.forEach(function(card) {
                        const nameElement = card.querySelector('.card-title');
                        if (nameElement) {
                            const textValue = nameElement.textContent || nameElement.innerText;
                            const isVisible = textValue.toLowerCase().indexOf(filter) > -1;
                            card.style.display = isVisible ? '' : 'none';
                            if (isVisible) mobileVisibleCount++;
                        }
                    });
                    
                    // Update results indicator for mobile
                    updateResultsIndicator(mobileVisibleCount, 'mobile');
                });

                // Enhanced search input styling
                searchInput.addEventListener('focus', function() {
                    this.style.boxShadow = '0 0 0 0.25rem rgba(25, 46, 47, 0.25)';
                });
                
                searchInput.addEventListener('blur', function() {
                    this.style.boxShadow = '';
                });
            }

            // Function to update search results indicator
            function updateResultsIndicator(count, view) {
                // Remove existing indicators
                const existingIndicators = document.querySelectorAll('.search-results-indicator');
                existingIndicators.forEach(el => el.remove());
                
                if (searchInput.value.length > 0) {
                    const indicator = document.createElement('div');
                    indicator.className = 'search-results-indicator mt-2';
                    indicator.innerHTML = `
                        <small class="text-primary fw-medium">
                            <i class="bi bi-search me-1"></i>
                            Found ${count} result${count !== 1 ? 's' : ''} for "${searchInput.value}"
                        </small>
                    `;
                    
                    if (view === 'desktop' && table) {
                        table.parentNode.insertBefore(indicator, table);
                    } else if (view === 'mobile' && mobileCards.length > 0) {
                        const mobileContainer = document.querySelector('.d-lg-none');
                        if (mobileContainer) {
                            mobileContainer.insertBefore(indicator, mobileContainer.firstChild);
                        }
                    }
                }
            }

            // Enhanced touch-friendly hover effects for mobile with theme colors
            if ('ontouchstart' in window) {
                const mobileItems = document.querySelectorAll('.category-mobile-item');
                mobileItems.forEach(function(item) {
                    item.addEventListener('touchstart', function() {
                        this.style.backgroundColor = 'rgba(25, 46, 47, 0.05)';
                        this.style.borderLeftColor = '#192E2F';
                        this.style.borderLeftWidth = '4px';
                    });
                    item.addEventListener('touchend', function() {
                        setTimeout(() => {
                            this.style.backgroundColor = '';
                            this.style.borderLeftColor = '';
                            this.style.borderLeftWidth = '';
                        }, 200);
                    });
                });

                // Add touch feedback for action buttons
                const actionButtons = document.querySelectorAll('.btn');
                actionButtons.forEach(function(btn) {
                    btn.addEventListener('touchstart', function() {
                        this.style.transform = 'scale(0.98)';
                    });
                    btn.addEventListener('touchend', function() {
                        setTimeout(() => {
                            this.style.transform = '';
                        }, 100);
                    });
                });
            }

            // Enhanced desktop table row hover effects
            const tableRows = document.querySelectorAll('.category-table-row');
            tableRows.forEach(function(row) {
                row.addEventListener('mouseenter', function() {
                    this.style.backgroundColor = 'rgba(25, 46, 47, 0.03)';
                    this.style.borderLeft = '4px solid #192E2F';
                });
                row.addEventListener('mouseleave', function() {
                    this.style.backgroundColor = '';
                    this.style.borderLeft = '';
                });
            });

            // Confirmation for delete actions with enhanced styling
            const deleteLinks = document.querySelectorAll('a[href*="Delete"]');
            deleteLinks.forEach(function(link) {
                link.addEventListener('click', function(e) {
                    const categoryName = this.closest('tr, .category-mobile-item')
                        ?.querySelector('.fw-medium, .card-title')?.textContent?.trim();
                    
                    const message = categoryName 
                        ? `⚠️ Delete "${categoryName}"?\n\nThis action cannot be undone.`
                        : '⚠️ Are you sure you want to delete this category?\n\nThis action cannot be undone.';
                    
                    if (!confirm(message)) {
                        e.preventDefault();
                    }
                });
            });

            // Dynamic stats update (if applicable)
            function updateStats() {
                const totalCategories = @Model.Count();
                const statsCards = document.querySelectorAll('.card-title');
                if (statsCards.length > 0) {
                    statsCards[0].textContent = totalCategories;
                }
            }

            // Initialize tooltips for better UX (if Bootstrap tooltips are available)
            if (typeof bootstrap !== 'undefined' && bootstrap.Tooltip) {
                const tooltipElements = document.querySelectorAll('[data-bs-toggle="tooltip"]');
                tooltipElements.forEach(el => new bootstrap.Tooltip(el));
            }

            // Performance optimization: Intersection Observer for lazy loading effects
            if ('IntersectionObserver' in window) {
                const observer = new IntersectionObserver((entries) => {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            entry.target.style.opacity = '1';
                            entry.target.style.transform = 'translateY(0)';
                        }
                    });
                }, { threshold: 0.1 });

                // Apply to stats cards for smooth entrance animation
                const statsCards = document.querySelectorAll('.col-6, .col-12');
                statsCards.forEach((card, index) => {
                    card.style.opacity = '0';
                    card.style.transform = 'translateY(20px)';
                    card.style.transition = `opacity 0.6s ease ${index * 0.1}s, transform 0.6s ease ${index * 0.1}s`;
                    observer.observe(card);
                });
            }
        });
    </script>
}

